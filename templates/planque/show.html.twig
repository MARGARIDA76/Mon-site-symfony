{% extends 'base.html.twig' %}

{% block title %}Planque{% endblock %}

{% block body %}
    <h1>Planque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planque.id }}</td>
            </tr>
            <tr>
                <th>Code_planque</th>
                <td>{{ planque.codePlanque }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ planque.adresse }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ planque.pays }}</td>
            </tr>
            <tr>
                <th>Type_planque</th>
                <td>{{ planque.typePlanque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planque_index') }}">back to list</a>

    <a href="{{ path('app_planque_edit', {'id': planque.id}) }}">edit</a>

    {{ include('planque/_delete_form.html.twig') }}
{% endblock %}
